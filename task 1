#include <stdio.h>

long get_fibbonachi_vai_cycle(int number)
{
  if (number == 1 || number == 2)
  return 1;

  return get_fibbonachi_vai_cycle(number - 1) +
  get_fibbonachi_vai_cycle(number - 2);
}

int main()
{
  int N;

  printf("N=");
  scanf("%d", &N);

  for (int i = 1; i <= N; i++)

  if (i!=N);
  else printf("%ld ", get_fibbonachi_vai_cycle(i));

  getchar();
  getchar();

  return 0;
}
